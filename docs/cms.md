# CMS architecture

How content authoring works in this project. Read this first when adding a schema, debugging an editor experience issue, or wiring a new page to Sanity.

For the rollout that built this system, see [`cms-rollout-plan.md`](./cms-rollout-plan.md) (delete once the rollout is complete and this doc captures the final state). For decision rationale, see [`adr/`](./adr/).

## Overview

Sanity is the source of truth for content. The Studio is embedded at `/studio` in this same Next.js app — schema + GROQ + the React component that reads them all change in a single PR ([ADR 0006](./adr/0006-embedded-sanity-studio.md)).

What's authored in Sanity, what's static:

| Surface | Authored where |
|---|---|
| Editions (year pages) | Sanity, with `src/data/editions/YYYY.ts` as a fallback during migration |
| Edition 2021 (online-only) | Static — never migrated to Sanity (one-off shape, no editorial value to re-author) |
| Artists | Sanity (`artist` doc) — currently surfaced only via the edition page; standalone artist routes pending |
| Organizations | Sanity (`organization` doc) — referenced from edition credits |
| Homepage | Sanity singleton `homepage` (in progress) |
| About, Partners, Visit, Privacy | Sanity singletons (in progress) |
| Press appearances, releases | Sanity docs `pressAppearance`, `pressRelease` (in progress) |
| Press kit assets (posters, covers) | `pressKit` object on each Edition doc (in progress) |
| Footer links, contact, social | Sanity singleton `siteSettings` (in progress) |
| Navigation labels | Hard-coded in `src/components/Navigation/Navigation.tsx` — 4 items, stable |
| Blog | Out of scope for now |

## Repo layout

```
sanity.config.ts                  # Studio config (basePath /studio, plugins, document actions)
sanity.cli.ts                     # CLI config (typegen settings)
schema.json                       # Generated by `sanity schema extract`
sanity.types.ts                   # Generated by `sanity typegen generate`
src/
  app/
    studio/[[...tool]]/           # The embedded Studio route
    api/draft-mode/{enable,disable}/route.ts
    (other routes)/               # Pages that read from Sanity
  sanity/
    env.ts                        # Reads NEXT_PUBLIC_SANITY_* env vars
    structure.ts                  # Desk structure tree
    schemaTypes/
      index.ts                    # Registers every type
      documents/                  # One file per document type
      objects/                    # One file per nested object type
    lib/
      client.ts                   # The Sanity client (useCdn: true, stega enabled)
      token.ts                    # SANITY_API_READ_TOKEN, typed string
      singleton.ts                # SINGLETON_TYPES registry + structure helper
      presentation.ts             # Document locations for the Presentation tool
      queries.ts                  # defineQuery-typed GROQ
      editions.ts                 # Server-only mapper: SanityEdition → Edition (the runtime shape)
      image.ts                    # urlFor() — image URL builder
      live.ts                     # Reserved for SanityLive (step 1.5)
  data/editions/                  # Static-fallback edition files (one per year)
  types/edition.ts                # Runtime edition shape (Sanity is mapped to this)
```

## Schemas

### Document types

`edition`, `artist`, `organization` are the current shipped documents. Future-state: `siteSettings`, `homepage`, `aboutPage`, `partnersPage`, `visitPage`, `privacyPage` singletons, plus `pressAppearance`, `pressRelease`.

### Conventions

- **Always use `defineType` / `defineField` / `defineArrayMember`** — pure objects break TypeGen. Already enforced in existing schemas.
- **Always assign an icon** from `@sanity/icons`. Improves Studio readability at scale.
- **Validate at the schema level**, not in the component. Required, length, regex, custom — all live in the field def.
- **Use field groups on documents with > 6 fields** (see `edition`). Tabs the editor experience instead of one long scroll.
- **Reverse-references**: when document A is commonly viewed in the context of document B, add a custom view to A showing referencing Bs (`S.view.component` or built-in `referencingDocuments`). Coming for `artist`.

### Rich text

Default: **plain paragraph arrays** (`array of strings`). One Sanity field per content block (hero copy, body, pillars). The renderer treats each entry as a `<p>`.

Exception: **`privacyPage.body` uses Portable Text** because it needs inline `<strong>` (cookie names) and `<a>` (legal links) which paragraph arrays can't carry. See [ADR 0007](./adr/0007-plain-paragraphs-over-portable-text.md).

If you're tempted to add Portable Text to a new schema, ask whether the page actually needs inline formatting that can't be modeled as separate fields (a "headline" field + a "body" field + a "links" array is usually cleaner than one big PT blob).

## Studio structure tree

Defined in `src/sanity/structure.ts`. Current shape:

```
Content
  ├─ Editions
  ├─ ────────
  ├─ Artists
  ├─ Organizations
  └─ (auto-included non-singleton types)
```

Future shape, after the rollout:

```
Content
  ├─ Site settings        (singleton)
  ├─ Homepage             (singleton)
  ├─ About                (singleton)
  ├─ Partners             (singleton)
  ├─ Visit                (singleton)
  ├─ Privacy              (singleton)
  ├─ ────────
  ├─ Editions
  ├─ ────────
  ├─ Artists
  ├─ Organizations
  ├─ ────────
  └─ Press
       ├─ Press appearances
       └─ Press releases
```

Singletons are pinned at the top because editors visit them less often than they visit collections, but when they do, the doc should be one click away — not buried in an alphabetical list.

## Singleton pattern

Sanity has no `singleton: true` schema option. We enforce singletons in three places ([ADR 0009](./adr/0009-singleton-pattern.md)):

1. **Structure tree** (`src/sanity/structure.ts` via `singletonListItem(S, type, title)`): renders the doc directly with a fixed `documentId === type`. Editors can't navigate to a "list view" of singletons.
2. **`document.actions`** (in `sanity.config.ts`): strips `delete` / `unpublish` / `duplicate` actions for singleton types. Editors can't accidentally destroy the homepage.
3. **`document.newDocumentOptions`** (in `sanity.config.ts`): removes singletons from the global "Create new" menu so editors can't create a second instance.

The single source of truth for which types are singletons is `SINGLETON_TYPES` in `src/sanity/lib/singleton.ts`. Adding a name there enables all three guards.

### Adding a new singleton

1. Define the schema document type as usual (`defineType({ name: 'fooPage', type: 'document', ... })`).
2. Register it in `src/sanity/schemaTypes/index.ts`.
3. Add `'fooPage'` to `SINGLETON_TYPES` in `src/sanity/lib/singleton.ts`.
4. Add it to the structure tree in `src/sanity/structure.ts`:
   ```ts
   singletonListItem(S, 'fooPage', 'Foo page'),
   ```
5. Query by id, not type: `*[_id == "fooPage"][0]` (faster, no need for `[0]` semantics on collections).
6. Add a Presentation `locations` entry in `src/sanity/lib/presentation.ts` so editors can jump from doc → live URL.

## Editor UX principles

Pulled from the audit and applied as the rollout progresses.

1. **Schema validates before editor publishes**, not after readers see it. Required fields, substring-match validations, length checks — all in the field def.
2. **Field descriptions explain WHY**, not WHAT (the name and type already say what). Bad: `description: 'The title field.'`. Good: `description: 'Shown as the page H1 and in the browser tab.'`.
3. **No newline-as-separator strings.** If a field is a list, model it as an array. Editors should never have to know that the renderer splits on `\n`.
4. **No same-string-typed-twice patterns** (e.g. `theme` + `themeHighlight` where the latter must be a substring of the former). Validate or restructure.
5. **Field groups** for documents with more than ~6 fields, so editors don't scroll a wall.
6. **Previews always carry context.** `preview.select` + `prepare` should make every list row identifiable without opening it. Already strong on `edition`, `programBlock`, `carouselImage`; needs love elsewhere.
7. **Singletons can't be deleted or duplicated.** Enforced via the singleton pattern above.
8. **Reverse references** wherever an editor would ask "where else is this used?". Custom views on `artist` (which editions), `organization` (which credits) — coming.

## Live preview & Visual Editing

**Current state** (post step 1):
- Presentation tool wired with `edition` + `artist` locations. Editors can open a document and click "Open preview" to jump to the rendered URL.
- Draft mode routes (`/api/draft-mode/enable`, `/api/draft-mode/disable`) are live. The Presentation iframe enters draft mode but the rendered page still shows the published version (no draft-mode-aware fetch yet).
- `stegaClean` is applied at the SEO boundary so invisible Visual Editing characters don't leak into `<title>`, OG tags, or JSON-LD. Without this, search engines see polluted strings and click-through tanks.

**Step 1.5 will add:**
- `<SanityLive />` for real-time updates when published content changes.
- `<VisualEditing />` for click-to-edit overlays in draft mode.
- Draft-mode-aware fetch in `src/sanity/lib/editions.ts` so the Presentation iframe shows actual drafts.
- A route-group restructure (`app/(site)/`) so neither component mounts on `/studio` — Sanity explicitly warns against that (causes unexpected reloads).
- Bump `next-sanity` v12 → v13 for clean `cacheComponents` interop.

Until step 1.5 lands, editors can publish + see the result on the next request (or after revalidation), but cannot click-to-edit and cannot preview unpublished drafts on the public site. They can still review drafts inside the Studio form view.

## Stega and SEO

Stega = Sanity's mechanism for embedding invisible characters in strings so the Presentation tool knows which field a rendered string came from. Two rules in this project:

1. **Never let stega'd strings reach `<head>` or `<script type="application/ld+json">`.** Search engines render them as garbage. Already handled in `src/lib/seo.ts` — every metadata / JSON-LD string passes through `stegaClean()`.
2. **Never compare stega'd strings with `===`.** `"published" === stegaCleanedValue` works; `"published" === rawValueFromQuery` may not. If you're branching on a string field's value, call `stegaClean` first.

## Caching & revalidation

`src/sanity/lib/client.ts` uses `useCdn: true` (fast, ~brief delay). `src/sanity/lib/editions.ts` wraps queries with the `'use cache'` directive — Next.js caches the mapped result. Edition pages are statically prerendered at build time and survive until the next revalidation.

For live updates (post-publish), we'll add tag-based revalidation in step 1.5: Sanity webhook → `/api/revalidate/tag` → `revalidateTag('edition')`.

## Editions: Sanity-first with static fallback

`src/data/editions/index.ts` is the gateway. `getEdition(year)` queries Sanity first and falls back to `src/data/editions/YYYY.ts` if not found. `getAllEditionYears()` merges both sets.

This lets us migrate one year at a time without breaking pages. As each year is fully entered into Sanity, the static file becomes dead weight — delete when confident. (2021 is intentionally permanent in code: it's the online-only edition with a unique shape, no editorial value to re-author.)

## Environment variables

```
NEXT_PUBLIC_SANITY_PROJECT_ID
NEXT_PUBLIC_SANITY_DATASET
NEXT_PUBLIC_SANITY_API_VERSION
SANITY_API_READ_TOKEN     # viewer-access token for draft mode
SANITY_API_WRITE_TOKEN    # for the blob upload + migration scripts (not used at runtime)
```

All required. `env.ts` and `token.ts` throw at import time if anything is missing — fail fast at boot.

## TypeGen

Run `pnpm typegen` after schema or query changes. It runs `sanity schema extract` then `sanity typegen generate`, emitting `sanity.types.ts` at the repo root. Always commit the result.

Eventually `pnpm typegen:watch` will run alongside dev — pending package.json approval.

## Adding a new page wired to Sanity

Walk-through with a hypothetical `/contact` page:

1. **Schema** (`src/sanity/schemaTypes/documents/contactPage.ts`):
   ```ts
   import { EnvelopeIcon } from '@sanity/icons'
   import { defineField, defineType } from 'sanity'

   export const contactPage = defineType({
     name: 'contactPage',
     title: 'Contact page',
     type: 'document',
     icon: EnvelopeIcon,
     fields: [
       defineField({ name: 'title', type: 'string', validation: (r) => r.required() }),
       defineField({ name: 'body', type: 'array', of: [{ type: 'string' }] }),
     ],
   })
   ```
2. **Register** in `src/sanity/schemaTypes/index.ts`.
3. **Singleton?** Yes — add `'contactPage'` to `SINGLETON_TYPES`; add `singletonListItem(S, 'contactPage', 'Contact')` to the structure tree.
4. **Query** in `src/sanity/lib/queries.ts`:
   ```ts
   export const CONTACT_PAGE_QUERY = defineQuery(`*[_id == "contactPage"][0]{ title, body }`)
   ```
5. **`pnpm typegen`** — `CONTACT_PAGE_QUERY_RESULT` becomes typed.
6. **Server-only fetch helper** in `src/sanity/lib/`:
   ```ts
   import 'server-only'
   import { client } from './client'
   import { CONTACT_PAGE_QUERY } from './queries'

   export async function getContactPage() {
     'use cache'
     return client.fetch(CONTACT_PAGE_QUERY)
   }
   ```
7. **Presentation location** in `src/sanity/lib/presentation.ts`:
   ```ts
   contactPage: defineLocations({
     resolve: () => ({ locations: [{ title: 'Contact', href: '/contact' }] }),
   }),
   ```
8. **Page** (`src/app/contact/page.tsx`):
   ```tsx
   export default async function ContactPage() {
     const data = await getContactPage()
     return <main>{/* render */}</main>
   }
   ```
