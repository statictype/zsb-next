### Summary table

| id | file | axis | smell |
| --- | --- | --- | --- |
| API-1 | src/components/Footer/Footer.tsx | prop-api | Footer links bypass the shared Button wrapper and duplicate link composition. |
| DS-1 | src/components/Footer/Footer.recipe.ts | ds-hygiene | Footer repeats local meta typography instead of composing an existing text role. |
| DS-2 | src/components/Footer/Footer.recipe.ts | ds-hygiene | Footer uses a raw bracketed `auto` basis for one breakpoint. |
| STRUCT-1 | src/components/PageHero/PageHero.tsx | structure | PageHero splits one title animation contract between JSX and the recipe. |
| API-2 | src/components/PartnerBadge/PartnerBadge.tsx | prop-api | The PartnerBadge link has no stable accessible name. |
| STRUCT-2 | src/components/PartnerBadge/PartnerBadge.tsx | structure | PartnerBadge emits the same SVG `id` for every instance. |
| DS-3 | src/components/PartnerBadge/PartnerBadge.tsx | ds-hygiene | PartnerBadge's SVG text styling bypasses the tokenized recipe layer. |
| DS-4 | src/components/PartnerBadge/PartnerBadge.recipe.ts | ds-hygiene | PartnerBadge's size model is raw bracketed proportional geometry controlled by parent CSS vars. |
| DS-5 | src/components/PartnerBadge/PartnerBadge.recipe.ts | ds-hygiene | PartnerBadge uses raw inherit styling instead of a named design-system move. |
| DS-6 | src/components/PartnerBadge/PartnerBadge.recipe.ts | ds-hygiene | PartnerBadge hardcodes its hover spring as raw transition literals. |
| DEAD-1 | src/components/PartnerBadge/PartnerBadge.recipe.ts | dead-code | PartnerBadge exposes a color variant that no call site uses. |

### Detailed findings

#### API-1

- id: `API-1`
- file:line: `src/components/Footer/Footer.tsx:20`
- axis: `prop-api`
- smell: Footer links bypass the shared Button wrapper and duplicate link composition.
- evidence: `FooterLink` builds classes with `button({ variant: 'link' })` at `src/components/Footer/Footer.tsx:22`, and the privacy link repeats the same generated recipe call at `src/components/Footer/Footer.tsx:90`; meanwhile `CookieSettingsButton` correctly composes the site wrapper with `<Button variant="link">` at `src/components/CookieBanner/CookieSettingsButton.tsx:13`, and the wrapper's `asChild` contract exists specifically to merge button classes onto native `<a>`/`<Link>` children without wrappers at `src/components/ui/Button/Button.tsx:10`.
- recommendation: compose footer anchors and `Link`s through `<Button asChild variant="link">`, keeping `s.link`/`s.legalLink` as className overrides; this shrinks the direct `styled-system/recipes` dependency and leaves link rendering behavior unchanged.

#### DS-1

- id: `DS-1`
- file:line: `src/components/Footer/Footer.recipe.ts:94`
- axis: `ds-hygiene`
- smell: Footer repeats local meta typography instead of composing an existing text role.
- evidence: `colTitle`, `stamp`, `baseline`, and `legalLink` all restate pieces of the meta-label treatment between `src/components/Footer/Footer.recipe.ts:94` and `src/components/Footer/Footer.recipe.ts:156`; `textStyles.metaLabel` already owns `fontFamily: 'body'`, `fontSize: '2xs'`, uppercase, `letterSpacing: 'label'`, `fontWeight: 'semibold'`, and `color: 'muted'` at `src/design-system/tokens.ts:505`, and sibling recipes compose it directly at `src/app/(site)/visit/_components/VenuesView.recipe.ts:93`, `src/app/(site)/visit/_components/VenuesView.recipe.ts:115`, and `src/app/(site)/editions/[year]/_components/Calendar/FollowLinks.recipe.ts:14`.
- recommendation: extract the footer's wide-stamped meta treatment into a `footerMeta` textStyle and apply it to `colTitle`, `stamp`, `baseline`, and `legalLink`; this makes the repeated typography single-source while preserving the wider stamp tracking where needed.

#### DS-2

- id: `DS-2`
- file:line: `src/components/Footer/Footer.recipe.ts:103`
- axis: `ds-hygiene`
- smell: Footer uses a raw bracketed `auto` basis for one breakpoint.
- evidence: The current policy says "`[bracketed]` literals still scattered through the recipes are migration backlog, not sanctioned art direction" and "There is no load-bearing exception that stays raw" at `src/design-system/preset.ts:20`; `flexBasis: '[auto]'` at `src/components/Footer/Footer.recipe.ts:103` is the only raw bracketed style literal in Footer.
- recommendation: replace the breakpoint `flexBasis` override with a tokenized/layout composition, for example a two-row mobile grid that removes the `full` to `auto` basis switch; this removes Footer's remaining bracketed style literal with no intended visual change.

#### STRUCT-1

- id: `STRUCT-1`
- file:line: `src/components/PageHero/PageHero.tsx:26`
- axis: `structure`
- smell: PageHero splits one title animation contract between JSX and the recipe.
- evidence: The JSX applies `css({ animationStyle: 'enter' })` at `src/components/PageHero/PageHero.tsx:26`, while the recipe applies only `animationDelay: 'fast'` at `src/components/PageHero/PageHero.recipe.ts:22`; the homepage sibling keeps the same pair together in the recipe at `src/app/(site)/page.recipe.ts:89`.
- recommendation: merge `animationStyle: 'enter'` into the PageHero `title` slot beside `animationDelay`, then inline the JSX to `className={styles.title}` and drop the `css`/`cx` imports; this shrinks the component and keeps the rendered animation timing unchanged.

#### API-2

- id: `API-2`
- file:line: `src/components/PartnerBadge/PartnerBadge.tsx:10`
- axis: `prop-api`
- smell: The PartnerBadge link has no stable accessible name.
- evidence: The only textual content inside the link is inside an SVG marked `aria-hidden="true"` at `src/components/PartnerBadge/PartnerBadge.tsx:13`, and the remaining child is just the Remix icon at `src/components/PartnerBadge/PartnerBadge.tsx:28`; all call sites render `<PartnerBadge />` with no label prop (`src/app/(site)/page.tsx:97`, `src/app/(site)/page.tsx:138`, `src/app/(site)/partners/page.tsx:125`, `src/components/Footer/Footer.tsx:58`).
- recommendation: replace the anonymous link with a named link by adding a component-owned `aria-label` such as "Become a partner"; this changes only assistive-technology output, not visual rendering.

#### STRUCT-2

- id: `STRUCT-2`
- file:line: `src/components/PartnerBadge/PartnerBadge.tsx:16`
- axis: `structure`
- smell: PartnerBadge emits the same SVG `id` for every instance.
- evidence: The path id is hardcoded as `partnerCircle` at `src/components/PartnerBadge/PartnerBadge.tsx:16`, and the text path references that same id at `src/components/PartnerBadge/PartnerBadge.tsx:21`; the site layout always renders the footer at `src/app/(site)/layout.tsx:42`, while pages can render another badge in main content at `src/app/(site)/page.tsx:97`, `src/app/(site)/page.tsx:138`, or `src/app/(site)/partners/page.tsx:125`.
- recommendation: derive the SVG path id with `useId()` and feed the generated id into both `id` and `href`; this removes cross-instance DOM id collisions without changing the badge geometry.

#### DS-3

- id: `DS-3`
- file:line: `src/components/PartnerBadge/PartnerBadge.tsx:20`
- axis: `ds-hygiene`
- smell: PartnerBadge's SVG text styling bypasses the tokenized recipe layer.
- evidence: The SVG text inlines `fontSize="40"`, `fontWeight="600"`, and `letterSpacing="8"` at `src/components/PartnerBadge/PartnerBadge.tsx:20`, while the recipe already targets `& text` for `fill` and `fontFamily` at `src/components/PartnerBadge/PartnerBadge.recipe.ts:61`; the design-system has nearby token roles for `fontWeights.semibold` at `src/design-system/tokens.ts:202` and letter spacing tokens at `src/design-system/tokens.ts:192`.
- recommendation: extract the SVG text font size, weight, and tracking into the `textRing` recipe, tokenizing any badge-specific values that do not snap to the current scale; this makes the ring typography auditable by Panda and leaves the SVG path data as geometry only.

#### DS-4

- id: `DS-4`
- file:line: `src/components/PartnerBadge/PartnerBadge.recipe.ts:18`
- axis: `ds-hygiene`
- smell: PartnerBadge's size model is raw bracketed proportional geometry controlled by parent CSS vars.
- evidence: The policy says "derive one value from another via a token-backed CSS var instead of hardcoding both" and "a bracket is debt to remove" at `src/design-system/preset.ts:22`; PartnerBadge has six raw bracketed badge-size calcs at `src/components/PartnerBadge/PartnerBadge.recipe.ts:18`, six matching icon-size calcs at `src/components/PartnerBadge/PartnerBadge.recipe.ts:67`, and the parent scale knob leaks into callers at `src/app/(site)/page.recipe.ts:109`, `src/app/(site)/page.recipe.ts:183`, and `src/components/Footer/Footer.recipe.ts:74`.
- recommendation: replace the parent-authored `--partner-badge-scale` contract with PartnerBadge recipe size variants that set token-backed badge and icon dimensions together; this removes twelve bracketed calc literals and pulls the sizing API back inside the component.

#### DS-5

- id: `DS-5`
- file:line: `src/components/PartnerBadge/PartnerBadge.recipe.ts:33`
- axis: `ds-hygiene`
- smell: PartnerBadge uses raw inherit styling instead of a named design-system move.
- evidence: `color: '[inherit]'` at `src/components/PartnerBadge/PartnerBadge.recipe.ts:33` is raw migration debt under `src/design-system/preset.ts:20`; the same un-tokenized inheritance pattern appears in sibling components at `src/components/ui/SectionHeading/SectionHeading.recipe.ts:17`, `src/app/(site)/_components/FeaturedEvents.recipe.ts:151`, `src/components/EditionTheme/EditionTheme.recipe.ts:68`, and `src/app/(site)/editions/[year]/_components/Calendar/Calendar.recipe.ts:386`.
- recommendation: tokenize the repeated inheritance reset as a shared design-system value used by these link-like slots; this removes the badge's raw inherit literal and gives the repeated inheritance reset one design-system home.

#### DS-6

- id: `DS-6`
- file:line: `src/components/PartnerBadge/PartnerBadge.recipe.ts:48`
- axis: `ds-hygiene`
- smell: PartnerBadge hardcodes its hover spring as raw transition literals.
- evidence: The recipe comment calls out the "Elastic hover-scale" at `src/components/PartnerBadge/PartnerBadge.recipe.ts:44`, but the implementation is a bracketed full transition at `src/components/PartnerBadge/PartnerBadge.recipe.ts:48` plus a bracketed `linear()` timing function at `src/components/PartnerBadge/PartnerBadge.recipe.ts:51`; the design-system already names durations/easings at `src/design-system/tokens.ts:206` and animation styles at `src/design-system/tokens.ts:369`.
- recommendation: tokenize the badge elastic easing and compose the transition from named duration/easing tokens, keeping the `@supports` upgrade as a named token-backed branch; this removes both raw transition literals and deletes the explanatory comment's job.

#### DEAD-1

- id: `DEAD-1`
- file:line: `src/components/PartnerBadge/PartnerBadge.recipe.ts:78`
- axis: `dead-code`
- smell: PartnerBadge exposes a color variant that no call site uses.
- evidence: The only public prop is `variant?: 'light' | 'dark'` at `src/components/PartnerBadge/PartnerBadge.tsx:5`, and `dark` only changes `textRing` fill at `src/components/PartnerBadge/PartnerBadge.recipe.ts:81`; all current call sites render the default with no variant at `src/app/(site)/page.tsx:97`, `src/app/(site)/page.tsx:138`, `src/app/(site)/partners/page.tsx:125`, and `src/components/Footer/Footer.tsx:58`.
- recommendation: delete the unused `dark` variant and the public `variant` prop until a real light-ground call site exists; this shrinks the API surface with no current visual impact.
